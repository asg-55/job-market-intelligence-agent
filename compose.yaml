services:
  copilot:
    build:
      context: .
      target: runtime
    env_file:
      - path: .env
        required: false
    environment:
      LLM_BASE_URL: ${DOCKER_LLM_BASE_URL:-http://host.docker.internal:11434/v1}
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped

  worker:
    build:
      context: .
      target: runtime
    env_file:
      - path: .env
        required: false
    environment:
      LLM_BASE_URL: ${DOCKER_LLM_BASE_URL:-http://host.docker.internal:11434/v1}
    command: ["job-copilot", "watch", "--interval", "43200"]
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      disable: true

  bot:
    build:
      context: .
      target: runtime
    env_file:
      - path: .env
        required: false
    environment:
      LLM_BASE_URL: ${DOCKER_LLM_BASE_URL:-http://host.docker.internal:11434/v1}
    command: ["job-copilot", "telegram-bot"]
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      disable: true

  tests:
    build:
      context: .
      target: test
    profiles: ["test"]

  lint:
    build:
      context: .
      target: lint
    profiles: ["test"]

  ollama:
    image: ollama/ollama:latest
    profiles: ["local-llm"]
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped

volumes:
  ollama_data:
